#include <iostream>
#include <string>
using namespace std;

int main() {
    string input;
    getline(cin, input); // Accept input from Node.js
    cout << "C++ received: " << input << endl; // Respond with a message
    return 0;
}
